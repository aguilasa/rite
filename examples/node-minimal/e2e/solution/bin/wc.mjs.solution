#!/usr/bin/env node
import { readFileSync } from "node:fs";
import { countWords } from "../src/count.mjs";

const file = process.argv[2];
if (!file) {
  console.error("usage: node bin/wc.mjs <file>");
  process.exit(2);
}
console.log(countWords(readFileSync(file, "utf8")));
